name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - uses: mihails-strasuns/setup-dlang@v0
    - name: Run tests for x64
      run: dub test -- -s
    - name: Run tests for x86
      run: dub test --arch=x86 -- -s